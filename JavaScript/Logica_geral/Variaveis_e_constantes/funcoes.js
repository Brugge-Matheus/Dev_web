// Funções - São trechos de códigos que são rodados apenas quando são chamados, podendo ser chamado a qualuqer momento no código
// O return é utilizado para retornar os valors desejados, porem o return finaliza todos os processos da função, ou seja nada mais abaixo dela sera rodado

/*function teste() {
    console.log('Chamando a função teste'); // Função simples sendo criado, quando for chamada apresentara esse código
}

 teste()



// Função com parâmetros, os parâmetros são definidos quando chamamos a função, podendo assim uma função ter diversos valores de parâmetro
function teste2(nome, idade, genero) { 
    console.log(`Meu nome é ${nome}`);
    console.log(`Minha idade é ${idade}`);
    console.log(`Eu sou do sexo ${genero}`);
    
}

teste2('Matheus', 18 , 'masculino')

//------------//

// Return - Vai retornar um valor e depois vai encerrar o processo de código ignorando tudo que esta a baixo dele, serve para quando você precisa retornar apenas um valor e tambem para quando deseja encerrar um código
 function perguntarNome() {
    return 'Matheus'

}
console.log(perguntarNome()); 


// Outro exemplo de função com parâmetro
function DefenirNome(parametro) {
    if (parametro == 1) {
        console.log('Matheus');

    } else if (parametro == 2) {
        console.log('Kleber');

    } else {
        console.log('Outro nome');
    }
}

    DefenirNome(1)

//------------//

// Definir funções com variáveis - O nome da váriavel se torna o nome da função podendo ser chamada da mesma forma

const idade = function() {
    alert('Função chamada')
}

idade()

//------------//

// Criar uma função e chamar ela logo em seguida, se adiciona tudo dentro de um (), e ao final adiciona outro () para chamar instantaneamente a mesma (não é muito utilizado)
(function(){
    alert('Ola mundo')
}) ();
*/

//------------//



