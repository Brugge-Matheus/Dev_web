let number = 10.7777667;
let n = '25'

console.log(number.toString()); // Transforma em String

console.log(number.toFixed(3)); // Fixa somente uma quantidade de números depois da virgula (Usado para $ principalmente)

n = parseInt(n) + 5.5; // Transforma uma variavel em número interiro
n = parseFloat(n) + 5.5 // Transforma uma variavel em número inteiro
console.log(n);
