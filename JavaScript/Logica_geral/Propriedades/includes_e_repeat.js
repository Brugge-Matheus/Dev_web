// Includes
// Serve para procurar itens

let list = ['ovo', 'cafe', 'arroz', 'feijão']

console.log(list.includes('cafe'));

// Repeat
// Serve para repetir uma ação determinadas vezes

let confirm = 'confirmado '

console.log(confirm.repeat(5));