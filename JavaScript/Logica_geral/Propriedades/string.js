const nome = '       Matheus Brugge Milczwski';

console.log(nome.length); // Pega a quantidade de caracteres

console.log(nome.indexOf('Brugge')); // Pega a posição de um item

console.log(nome.slice(0, 12)); // Seleciona uma certa quantidade de itens

console.log(nome.replace('Brugge', 'Bruggezin')); // Substitui um item por outro

console.log(nome.trim());

let itens = 'Esmeralda,Safira,Diamante';
let resultado = itens.split(',')

console.log(itens);
console.log(resultado);




